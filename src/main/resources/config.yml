api:
  base-url: "https://your-app-domain.run.app"
  # Used to fetch only this server's pending orders.
  server-name: "servername"
  interval-seconds: 60

# Optional secret header sent with requests
api.secret: ""

# Auto-update settings
auto-update:
  enabled: true
  url: "https://raw.githubusercontent.com/luckypro387-commits/Rankapi/main/Auto%20Update/version.json"

# Allowed ranks enforced locally
allowed-ranks:
  - vip
  - mvp
  - legend

# Debug logging
debug: true
